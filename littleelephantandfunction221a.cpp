#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define forn(i, n) for (int i = 0; i < int(n); i++)

int main()
{
    ios_base::sync_with_stdio(NULL);
    cin.tie(NULL);
    cout.tie(NULL);

    ll n;
    cin>>n;
    //int arr[n];
    //arr[0] = n;
    cout<<n<<" ";
    for(int i=1;i<n;i++)
    {
        //arr[i]= i;
        cout<<i<<" ";
    }
    //arr[n-1] = n-1;
    //cout<<n-1;
    return 0;
}